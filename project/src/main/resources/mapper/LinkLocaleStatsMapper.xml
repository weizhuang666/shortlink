<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.shortlink.project.dao.mapper.LinkLocaleStatsMapper">

    <insert id="shortLinkLocaleStats">
        INSERT INTO
            t_link_locale_stats (
            full_short_url,
            gid,
            date,
            cnt,
            province,
            city,
            adcode,
            country,
            create_time,
            update_time,
            del_flag
        )
        VALUES(
                  #{LinkLocaleStats.fullShortUrl},
                  #{LinkLocaleStats.gid},
                  #{LinkLocaleStats.date},
                  #{LinkLocaleStats.cnt},
                  #{LinkLocaleStats.province},
                  #{LinkLocaleStats.city},
                  #{LinkLocaleStats.adcode},
                  #{LinkLocaleStats.country},
                  NOW(),
                  NOW(),
                  0
              ) ON DUPLICATE KEY
        UPDATE
            cnt = cnt + #{LinkLocaleStats.cnt}
    </insert>

</mapper>