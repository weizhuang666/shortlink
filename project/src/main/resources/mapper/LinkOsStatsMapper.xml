<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.shortlink.project.dao.mapper.LinkOsStatsMapper">

    <insert id="shortLinkOsStats">
        INSERT INTO
            t_link_os_stats (
            full_short_url,
            gid,
            date,
            cnt,
            os,
            create_time,
            update_time,
            del_flag
        )
        VALUES(
                  #{LinkOsStats.fullShortUrl},
                  #{LinkOsStats.gid},
                  #{LinkOsStats.date},
                  #{LinkOsStats.cnt},
                  #{LinkOsStats.os},
                  NOW(),
                  NOW(),
                  0
              ) ON DUPLICATE KEY
        UPDATE
            cnt = cnt + #{LinkOsStats.cnt}
    </insert>

</mapper>